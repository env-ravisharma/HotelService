server:
  port: 8082

spring:
  application:
    name: HOTEL-SERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/hotel-service
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true